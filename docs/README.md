# docs

Documentation and distribution artifacts for the PACES Workbook Generator.

## Contents

- **[`transfer/`](transfer/)** — Code transfer packages: Word documents that carry the repo's Python scripts across a locked-down security boundary as text, for firewalled environments where executable files cannot be moved directly. See [`transfer/README.md`](transfer/README.md) for details and verification hashes.
